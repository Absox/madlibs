.site-content {
	height: "calc(100vh - %s - %s)" % (header_height footer_height);
	overflow:hidden;
	min-height:400px;
}

.site-content__centered-wrapper {
	height:100%;
	display:flex;
	align-items: center;
    justify-content: center;
}